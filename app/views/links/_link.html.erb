<div class="link">
	<h4><%= link_to link.title, link.uri %></h4>
    <p><%= link.description %></p>
    <p>Tags: <%= raw link.tag_list.map { |t| link_to t, tag_path(t) }.join(', ') %></p>
    <p><%= link.private %></p>
    <p><%= link.user.username rescue "No user" %></p>
    <p><%= link_to 'Edit', edit_link_path(link) %></p>
    <p><%= link_to 'Destroy', link, method: :delete, data: { confirm: 'Are you sure?' } %></p>
</div>