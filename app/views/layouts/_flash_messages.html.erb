<div id="flash">
  <% flash.each do |key, value| %>
    <div class="<%= flash_class(key) %>">
      <div class="button close-white pull-right"><span>Close</span></div>
      <%= value %>
    </div>
  <% end %>
</div>